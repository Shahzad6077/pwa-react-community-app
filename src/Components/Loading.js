import React from "react";

const Loading = () => {
  return (
    <div className="bg-teal-lighter fixed top-0 inset-0 max-h-screen max-w-full flex">
      <p className="m-auto">LOADING...</p>
    </div>
  );
};

export default Loading;
